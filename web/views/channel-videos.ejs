<!DOCTYPE html>
<html lang="ko">
  <head>
    <title><%= channel.title %> - 최신 영상</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <link rel='stylesheet' href='/css/style.css' />
    <script src="/js/index.js"></script>
  </head>
  <body>
    <section id="title">
      <h1 class="title">
        <a href="/@<%= user.username %>">
          <img src="<%= channel.thumbnail %>" alt="<%= channel.title %>" class="profile" style="vertical-align: middle; margin-right: 0.5rem;" />
          <%= channel.title %>
        </a>
      </h1>
    </section>
    <%- include('nav.ejs') %>
    
    <p style="padding: 0 1rem;">
      <a href="https://www.youtube.com/<%= channel.customUrl %>" target="_blank"><%= channel.customUrl %></a> 채널의 최신 영상 목록입니다.
    </p>
    
    <p style="padding: 0 1rem;">
      <a href="/@<%= user.username %>">← 내 피드로 돌아가기</a>
    </p>

    <hr>

    <ul id="list">
      <% if (videos && videos.length > 0) { %>
        <% videos.forEach(function(video) { %>
          <li data-v="<%= video.videoId %>">
            <a href="https://youtu.be/<%= video.videoId %>" alt="<%= video.title %>" target="_blank">
              <img src="<%= video.thumbnail %>" alt="<%= video.title %>" loading="lazy" />
              <div>
                <%= video.pubdate %>
              </div>
              <%= video.title %>
            </a>
          </li>
        <% }); %>
      <% } else { %>
        <p style="padding: 1rem;">이 채널에 영상이 없습니다.</p>
      <% } %>
    </ul>
    <%- include('footer') %>
  </body>
</html>
