<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>프로필 - <%= user.username %></title>
  <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
  <link rel="manifest" href="/favicon/site.webmanifest">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
  <%- include('nav.ejs') %>
  <section id="title">
    <h1 class="title">
      <a href="/@<%= user.username %>">
        <img src="<%= user.photo %>" alt="<%= user.username %>" class="profile" style="vertical-align: middle; margin-right: 0.5rem; width: 48px; height: 48px; border-radius: 50%;" />
        <%= user.username %> 프로필
      </a>
    </h1>
  </section>

  <section style="padding: 1rem;">
    <h3>기본 정보</h3>
    <table class="channels">
      <tbody>
        <tr>
          <th style="width: 120px;">사용자명</th>
          <td><%= user.username %></td>
        </tr>
        <tr>
          <th>이름</th>
          <td><%= user.displayName %></td>
        </tr>
        <% if (user.emails && user.emails[0]) { %>
        <tr>
          <th>이메일</th>
          <td><%= user.emails[0].value %></td>
        </tr>
        <% } %>
        <tr>
          <th>구독 등급</th>
          <td><%= user.subscriptionTier || 'free' %></td>
        </tr>
      </tbody>
    </table>
  </section>

  <section style="padding: 1rem;">
    <h3>바로가기</h3>
    <nav style="display: flex; gap: 1rem; flex-wrap: wrap;">
      <a href="/@<%= user.username %>" class="btn">내 피드</a>
      <a href="/@<%= user.username %>/manage" class="btn">채널 관리</a>
      <a href="/admin" class="btn">관리자</a>
      <a href="/logout" class="btn" style="background: #dc3545;">로그아웃</a>
    </nav>
  </section>
  <%- include('footer') %>
</body>
</html>
